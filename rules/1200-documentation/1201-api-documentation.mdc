---
id: 1201
title: API 文档最佳实践
description: API 文档编写、维护和发布的最佳实践
globs: ["**/docs/**/*.md", "**/README.md", "**/*.openapi.yml", "**/swagger.json"]
priority: medium
tags: [documentation, api-docs, openapi, swagger, markdown, readme]
version: 1.0.0
author: Turbo AI Rules
lastUpdated: 2025-11-20
---

# API 文档最佳实践

## 关键原则

- ✅ 文档即代码（OpenAPI 规范）
- ✅ 多语言示例（cURL, JavaScript, Python）
- ✅ 交互式文档（Swagger UI/Redoc）
- ✅ 版本化文档

## OpenAPI 规范关键点

```yaml
openapi: 3.0.0
info:
  title: API Title
  version: 1.0.0
  contact:
    email: api@example.com

servers:
  - url: https://api.example.com/v1

paths:
  /users:
    get:
      operationId: listUsers # 必须唯一
      tags: [Users]
      parameters:
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        "200":
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UserList" }
              examples:
                success: { value: { data: [...] } }
      security:
        - bearerAuth: []

components:
  schemas:
    User:
      required: [id, email]
      properties:
        id: { type: string }
        email: { type: string, format: email }

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
```

## README 结构

```markdown
# 项目名称

> 一句话描述

## 特性

- 🚀 核心特性 1

## 快速开始（3 步以内）

## API 参数

| 名称 | 类型   | 必填 | 默认值 | 描述 |
| ---- | ------ | ---- | ------ | ---- |
| port | number | 否   | 3000   | 端口 |
```

## 代码示例

```bash
# cURL
curl -X POST https://api.example.com/users \
  -H "Authorization: Bearer TOKEN" \
  -d '{"name":"John"}'
```

```javascript
// JavaScript
const res = await fetch(url, {
  method: "POST",
  headers: { Authorization: `Bearer ${token}` },
  body: JSON.stringify(data),
});
```

## 错误文档

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Invalid input",
    "details": { "field": "email" },
    "requestId": "req_abc123"
  }
}
```

| 错误码              | HTTP | 解决方案           |
| ------------------- | ---- | ------------------ |
| AUTH_REQUIRED       | 401  | 添加 Authorization |
| RATE_LIMIT_EXCEEDED | 429  | 降低请求频率       |

## 文档工具

**Redoc（美观）：**

```html
<redoc spec-url="./openapi.yml"></redoc>
<script src="https://cdn.redoc.ly/redoc/latest/bundles/redoc.standalone.js"></script>
```

**Swagger UI（交互式）：**

```yaml
# docker-compose.yml
services:
  swagger-ui:
    image: swaggerapi/swagger-ui
    environment:
      SWAGGER_JSON: /openapi/openapi.yml
```

## 版本化策略

```
/docs/v1/           # 旧版本
/docs/v2/           # 当前版本
/docs/latest/       # 链接到最新
```

## 项目配置

```json
{
  "devDependencies": {
    "@redocly/cli": "^1.0.0",
    "swagger-ui-express": "^5.0.0"
  }
}
```
