---
id: 1101
title: Git 工作流最佳实践
description: Git 分支策略、提交规范和团队协作的最佳实践
globs: ["**/.git/**", "**/.gitignore", "**/.gitattributes"]
priority: medium
tags: [git, version-control, branching, commit-convention, workflow]
version: 1.0.0
author: Turbo AI Rules
lastUpdated: 2025-11-20
---

# Git 工作流最佳实践

## 适用场景

- 团队协作开发、版本控制管理、代码审查流程

## 核心内容

GitHub Flow（推荐）、Conventional Commits 提交规范、PR 流程、Husky + lint-staged 自动化。

## 关键原则

- ✅ 频繁提交,小步迭代
- ✅ Conventional Commits 提交信息
- ✅ 分支命名规范
- ✅ 使用 Pull Request
- ✅ 保持主分支稳定

## 关键决策点

### 1. GitHub Flow 分支策略

```bash
# 从 main 创建分支
git checkout main && git pull
git checkout -b feature/add-user-profile

# 开发和提交
git push origin feature/add-user-profile
# 创建 PR,审查通过后合并
```

**分支命名规范：**

```
类型/范围-描述

✅ feature/user-authentication
✅ bugfix/login-validation
✅ hotfix/payment-error
❌ new-feature, my-branch
```

### 2. Conventional Commits 规范

```bash
git commit -m "feat(auth): implement JWT authentication"
git commit -m "fix(ui): correct button alignment"
git commit -m "docs: update API documentation"

# Breaking Change
git commit -m "feat(api): change user endpoint

BREAKING CHANGE: /api/user -> /api/v2/users"
```

### 3. PR 模板

**.github/pull_request_template.md：**

```markdown
## 📝 变更描述

## 🔗 相关 Issue
Closes #123

## ✅ 检查清单
- [ ] 代码符合规范
- [ ] 测试已通过
- [ ] 文档已更新
```

### 4. Husky + lint-staged

```json
{
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "lint-staged": {
    "*.{js,ts}": ["eslint --fix", "prettier --write"]
  }
}
```

### 5. commitlint配置

```javascript
module.exports = {
  extends: ['@commitlint/config-conventional'],
  rules: {
    'type-enum': [2, 'always', ['feat', 'fix', 'docs', 'refactor', 'test', 'chore']],
  }
};
```

## 项目特定配置

**.gitignore：**

```
node_modules/
dist/
.env
coverage/
.DS_Store
```

## 常见问题

**Q: rebase vs merge？**
A: Feature 分支 rebase 保持线性,合并主分支用 merge 保留历史。

## 扩展阅读

- [GitHub Flow](https://guides.github.com/introduction/flow/)
- [Conventional Commits](https://www.conventionalcommits.org/)
